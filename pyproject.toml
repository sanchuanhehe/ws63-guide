[project]
name = "ws63-user-guide"
version = "01"
requires-python = ">=3.10"
dependencies = [
    "sphinx>=7.0",
    "myst-parser>=3.0",
    "sphinx-rtd-theme>=2.0",
    "sphinxcontrib-mermaid>=0.9",
]

[tool.uv]
dev-dependencies = []
